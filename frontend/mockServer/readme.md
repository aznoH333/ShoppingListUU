# How to run?
1. install mock-json-server ``npm install -g mock-json-server``
2. run mock-json-server in the mockServer directory ``mock-json-server data.json``


The application expects the server to be running on the port 8000
